# Task 4: Verification

Review the completed state, backend, and CLI changes. Add focused tests only if the repository has an established test harness for these modules. Cover normalization defaults, adjacent EVA movement rejection, positive collection validation, and alert acknowledgement. Run the full available test suite plus `bunx tsc -p tsconfig.json --noEmit` and CLI `--help` smoke checks. Write `.superpowers/sdd/task-4-report.md` with exact commands and results; do not change production behavior unless a test exposes a real defect.
