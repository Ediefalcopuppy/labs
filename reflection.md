1. My 'labs'-like folder's parent directory is my downloads folder, while the 'terminal-navigation-walkthrough'-like folder is in the 'labs'-like folder. 
2. ~ is the home directory.
3. I used commands such as `cd`, `ls`, and `pwd`.
4. I can verify if folders or files were created by using `ls`.
5. Hidden files or folders are hidden from the normal `ls` command, but can be viewed using `ls -a`.
6. Moving into a folder called 'notes' requires you to type the exact name of the folder, but to go to a parent directory, you can use `cd ..`.
7. . means the current directory, while .. means the parent directory.
8. Tab completion helps save time by automatically entering names of files or folders.
9. You can search your command history by using the up and down arrow keys, or by using the `history` command.
10. I redirected the output of `grep` from my `history` command to a file called 'a.txt' by using the `>` operator.
11. I created 'reflection.md' by using the `touch` command.
12. Guessing in the terminal is risky because you could accidentally delete or mess with files on accident.